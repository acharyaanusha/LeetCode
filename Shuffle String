class Solution:
    def restoreString(self, s: str, indices: List[int]) -> str:
        n =len(indices)
        t=['']*n
        for i in range(n):
             t[indices[i]]=s[i]
        return ''.join(t)
        
