class Solution:
    def myAtoi(self,s:str)->int:
        match = re.match(r'^\s*([+-]?\d+)',s)
        if match:
            y = match.group(1)
        else:
            y = 0
        return min(max(y,-2**31),2**31-1)
